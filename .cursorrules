
# REGLAS DE PROYECTO - AULA GENIA

## 🚨 ZONAS CRÍTICAS (REQUIEREN CONFIRMACIÓN)
Si necesitas modificar código relacionado con **Meta Pixel**, **CAPI (Conversions API)** o **Google Analytics**, debes **pedir confirmación explícita al usuario** antes de aplicar los cambios.

Archivos y funciones clave protegidas:
- `functions/index.js` -> `trackViewContent`, `trackInitiateCheckout`, `sendMetaCAPIEvent`
- `landing-nuevo.html` -> Scripts de `fbq('init')`, `fbq('track')`, `gtag`
- `acceso.html` -> Lógica de Pixel

**Excepción:** Si el usuario pide explícitamente "arreglar el pixel" o "cambiar el pixel", puedes proceder sin preguntar de nuevo.

## ESTILO Y NOMBRE
- Mantener nombres de variables en inglés/español consistente con el archivo.
- No borrar comentarios de advertencia existentes.
